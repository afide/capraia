description = "The $project.name module"

dependencies {
    compile(
            project(":modules:$project.rootProject.name-jabci"),
            project(":modules:$project.rootProject.name-merkletree"),
            project.capraia.modules.jackson,
    )
}
